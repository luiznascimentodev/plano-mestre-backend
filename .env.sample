# Preencha e renomeie para .env (NÃO COMMITAR .env real)

# === Required ===
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public
JWT_SECRET=coloque_um_segredo_longo_aqui

# === App ===
NODE_ENV=production
PORT=3001
ALLOWED_ORIGINS=https://plano-mestre-frontend.vercel.app
FRONTEND_URL=https://plano-mestre-frontend.vercel.app
COOKIE_SAMESITE=none
COOKIE_SECURE=true
# COOKIE_DOMAIN=api.seudominio.com

# === Observações ===
# COOKIE_SAMESITE=none exige HTTPS + COOKIE_SECURE=true para navegadores modernos.
# Ajuste ALLOWED_ORIGINS caso tenha múltiplos domínios separados por vírgula.